class Main {
    let i : int;

    main () : int {
        let z : int[];
        let res : int;
        let j : int;
        let floatNum : real;
        let message : string;
        j <- in_int();
        i <- 1;
        z <- new Array(int, 100);
        z[i] <- 5;
        res <- z[i] * 5;
        message <- in_string();
        floatNum <- in_real();
        out_string(message);
        out_int(res + 5);
        out_string(" ");
        out_real(floatNum);
    }
}