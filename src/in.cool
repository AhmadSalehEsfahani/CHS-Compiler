class Main {
    let i : int;

    main () : int {

        let message : string;
        let i : int;
        let j : int;


        i <- 5;
        j <- 0;

        while i > 0 loop
            j <- j + 1;
            i <- i - 1;
        pool

        message <- "j = ";
        out_string(message);
        out_int(j);
        message <- "i = ";
        out_string(message);
        out_int(i);

    }
}